import { getAllRegistry } from '@/api/registry/getAllRegistry'
import { getRegistry } from '@/api/registry/getRegistry'
import { createRegistry } from '@/api/registry/createRegistry'
import { updateRegistry } from '@/api/registry/updateRegistry'

export { getAllRegistry, getRegistry, createRegistry, updateRegistry }
